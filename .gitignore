# Mac OSX Files
.DS_Store
.Trashes
.Spotlight-V100
.AppleDouble
.LSOverride

# NPM / Yarn
node_modules
npm-debug.log
# dist and lib intentionally not ignored — the HappyFunCorp fork ships
# prebuilt artifacts so this repo is npm-installable directly from a git
# URL (`github:HappyFunCorp/react-popper#tag`) without a build step.
yarn-error.log
coverage

# General Files
.sass-cache
.hg
.idea
.svn
.cache
.project
.tmp
.vscode
*.log
